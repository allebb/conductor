[Definition]
# The conductor-manual jail is managed directly with fail2ban-client, but
# Fail2Ban still requires every jail filter to expose a valid failure-id group.
failregex = ^conductor-manual\s+<HOST>\s*$
ignoreregex =
