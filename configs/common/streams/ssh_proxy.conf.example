# Example TCP stream proxy.
# Rename to ssh_proxy.conf, then adjust to expose a local SSH service on an alternate port.
#
# stream {
#     server {
#         listen 2222;
#         listen [::]:2222;
#         proxy_pass 127.0.0.1:22;
#         proxy_connect_timeout 10s;
#         proxy_timeout 1h;
#     }
# }
